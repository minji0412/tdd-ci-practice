name: PR Check

on:
 pull_request:
  branches: [main]

premissons:
  contents: read


  jobs:
    quality:
      runs-on: ubuntu-latest
      
      steps:
      - name: Checkout
      uses: actions/setup-node@v6
      width:
        node-version: 24
        cache: npm

      -name: Install
      run: npm ci
      np
      -name: Test
      run: npm test
      
